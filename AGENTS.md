# ExcelBatchRenamer 项目记忆与协作规则

本文件适用于仓库根目录及全部子目录。所有人工开发者和自动化代理在修改项目前，都必须先阅读本文件。

## 1. 已确认的项目事实

- 仓库：`https://github.com/olelius/excel-batch-renamer`
- 默认分支：`main`
- 项目目标：根据 Excel 表格批量创建文件夹或修改文件夹名称，并根据文件夹批量修改 JPG 图片名称、按文件题名自动生成 PDF。
- 工具定位：受控流水线中的小型作业插件，输入由上游人工流程保证符合规范，不建设面向任意脏数据的通用清洗和恢复系统。
- 当前阶段（2026-09-16）：v0.4.2 为双目录编排增加“档号名称 Excel”输入，按档号补全 `001-文件夹名称`；输入表只含“档号、文件夹名称”，不含序号。8 项针对性检查通过，按用户要求未追加完整回归或审查；双 `onedir` 构建、便携运行与发布 ZIP 解压自检通过。真实离线 Windows 7 SP1 x64 仍待目标机验证。本次记录见 `docs/project-notes/2026-09-16-archive-name-delivery.md`。
- 已确认技术约束：使用 Python；客户机为 64 位 Windows 7 旗舰版 SP1；首版只读取 `.xlsx`；交付物复制到客户机后可直接双击打开 UI，无需安装 Python、编程语言或其他开发环境。
- 已确认交付方式：使用完整便携目录交付，用户必须复制整个目录并双击其中的 EXE；不依赖安装程序，不要求把全部内容压成单个 EXE。
- 已确认构建配置：测试版和正式交付版都使用完整便携目录及同一代码；测试版启动时保留 CMD 控制台以显示日志和异常，正式交付版不得显示控制台，只显示 UI。
- 已确认打包工具：使用项目虚拟环境中的 PyInstaller 4.10 `onedir` 打包，不直接复制 `.venv`，不手工维护嵌入式 Python；客户机离线运行，不需要网络、pip 或外部 Python。
- 已确认离线依赖边界：交付目录必须包含全部应用依赖；不得要求客户机安装 .NET Framework、Java、Python、Visual C++ 运行库安装包、Microsoft Excel、WPS 或其他环境，也不得在运行时联网下载组件。
- 已确认 UI 技术：使用 Python 3.8 自带的 `Tkinter/ttk`；界面以清晰、可操作和稳定为目标，不追求复杂视觉效果，不使用 PySide6、PySide2 或 Qt。
- 已确认 Excel 技术：客户机的办公软件不确定，可能使用 WPS；程序必须使用 `openpyxl` 直接读取 `.xlsx`，不得依赖 Microsoft Excel、WPS、COM、宏或 Office 插件。
- 已确认表头位置：文件夹模板表头固定在第一行、数据从第二行开始；图片任务表表头固定在第三行、数据从第四行开始，图片工作表前两行内容忽略；程序不扫描或推测其他表头行。
- 已确认表头空白处理：匹配固定表头行前删除其中全部 Unicode 空白字符（包括普通空格、全角空格和制表符）；只规范化表头，不删除数据单元格内容中的空格。
- 已确认数据连续性：有效数据从各任务规定的数据起始行连续排列（文件夹第二行、图片第四行），不存在空白行后还有有效数据；读取时遇到首个完全空白数据行即可结束。
- 已确认操作边界：创建文件夹、重命名文件夹、单文件夹重命名 JPG 和多文件夹批量重命名 JPG 是四个相互独立的任务模式，每次运行只能选择并执行一种。
- 已确认目录边界：每个任务模式必须单独选择自己的任务目录；禁止设置一个由四种任务模式共享或固定复用的全局目录。文件夹创建任务只在其当前选择的任务目录下创建文件夹。
- 已确认文件夹命名规则：创建任务及旧式文件夹表仍使用 `序号——文件夹名称`；双目录编排根据档号映射分配从 001 开始的全局新序号，目标使用 `新序号-文件夹名称`，分隔符为半角 `-`。
- 已确认编号前提：文件夹序号和图片页码按三位数字补零；业务数据不会出现需要四位表示的编号，程序不需要增加上限校验或四位编号分支。
- 已确认文件夹任务工作表：文件夹创建与文件夹重命名使用的固定模板只有一个工作表，程序直接读取该工作表，UI 不提供工作表选择。
- 已确认占位流程：文件夹创建任务允许 `文件夹名称` 为空，此时创建 `001——` 形式的占位文件夹；文件夹重命名任务接受三位数字前缀或编码前缀的源文件夹，并按提取出的序号与任务表唯一匹配后生成标准三位数字名称。
- 已确认编码源文件夹：`I74-6-256——` 是待改名的源文件夹，取 `——` 前最后一段数字 256；任务表 `序号=256`、`文件夹名称=验收文件` 对应目标 `256——验收文件`。本次实现接受字母数字段以半角 `-` 连接、末段为正整数的编码；来源编码不保留到标准目标名称。空名称行保留当前文件夹，不单独清理前缀；同一序号匹配到多个源目录时仍拦截。
- 已确认稀疏重命名任务：文件夹重命名表可以只填写序号 256，不需要补齐 1 至 255；只匹配表格列出的序号，未列出的其他直属文件夹保持不变。该规则不影响文件夹创建任务的连续序号要求。
- 已确认双目录编排输入：用户选择两个不同 `.xlsx`，文件名分别包含“文件目录”和“图纸目录”，并选择图片任务父目录；原工作表名称就是档号，工作表标签位置不作为档号对应依据。
- 已确认档号名称输入：批量整理还必须选择一个“档号名称 Excel”，该表只包含 `档号`、`文件夹名称` 两列，不包含 `序号`；新序号由程序生成。文件夹名称必须取自该表，不从原文件夹尾部猜测。
- 已确认空名称修复：若前版已经生成 `001-` 这类空名称文件夹，新版可结合已有档号映射表和新选择的档号名称表补全为 `001-题名`。
- 已确认全局新序号：先为文件目录工作簿中的档案分配 `001...`，再为图纸目录工作簿接续编号；同一类别内按档号自然顺序生成映射，不按原工作表标签位置。程序生成 `档号与新序号对应表.xlsx`，再按档号查找并重命名对应工作表和文件夹。
- 已确认批量目标命名：文件夹按映射改为 `001-文件夹名称`；每个文件夹内直属 JPG 都按当前页码顺序从 `001` 独立重新起号，再按对应工作表的文件题名改名并生成 PDF。
- 本次实现默认的写入位置与重跑：两个目录工作簿原地更新工作表名称，档号映射表写入图片任务父目录；再次执行时通过已有映射表识别已编号工作表和文件夹。
- 已确认空名称处理：文件夹重命名任务遇到空的 `文件夹名称` 时，保留当前文件夹名称（包括已补名目录或来源编码前缀）并记录为无变更项，不执行文件系统重命名。
- 已确认图片任务输入：图片任务表可以包含多个工作表和多列，其中存在 `序号`、`文件题名` 和 `页次`；图片初始名称为三位页码（如 `001.jpg`、`002.jpg`），表内 `序号` 列不参与图片重命名。
- 已确认图片任务绑定：用户必须分别选择 `.xlsx`、其中一个数字名称工作表和一个可解析序号的文件夹。工作表名称仍使用不补零数字（如 `1`、`2`、`3`），其数值必须等于所选文件夹提取出的序号；例如 `001——` 对应 `1`，`I74-6-256——` 与 `256——验收文件` 均对应 `256`。图片任务只处理 JPG/PDF，文件夹改名由独立标签页执行。
- 已确认图片目标名称示例：图片页码直接拼接文件名称，例如 `001项目建议批复文件及项目建议书.jpg`，中间不使用分隔符。
- 已确认页次推导规则：单独页次表示当前文件的起始页，其结束页为下一行起始页减一；最后一行使用 `起始页-结束页` 范围明确结束页，区间首尾均包含。
- 已确认图片扫描范围：图片重命名只处理所选文件夹直接包含的 JPG，不递归进入子文件夹；业务上预期不存在子文件夹。
- 已确认图片重复执行：图片任务任何时候都允许重新执行；每次必须根据当前工作表和当前图片集合重新计算，图片数量、页次区间和文件名前三位页码均可能变化，不得依赖上一次执行记录。已重命名图片仍按其当前前三位页码匹配并重算目标名称。
- 已确认自动 PDF 需求：单文件夹和批量图片任务在图片改名后，把同一个文件的图片自动生成 PDF；PDF 名称固定为三位起始页直接拼接文件题名。例如 `验收文件`、`6-10` 生成 `006验收文件.pdf`，末行显式范围 `023-045` 只取起始页并生成 `023验收文件.pdf`。用户明确要求自动生成，不增加独立页面、开关或二次执行步骤，保留四个标签页。
- 本次实现默认的 PDF 归集与输出：同一工作表中完全相同题名的全部页码，即使来自不相邻数据行，也按页码升序归并为一个 PDF，保存于对应原图片文件夹并保留 JPG；不同项目文件夹分别生成，不跨文件夹归并。仅大小写不同而在 Windows 下产生同名歧义的题名在改名前拦截。
- 本次实现默认的 PDF 重跑：每次按当前工作表和图片集合重新生成并覆盖当前“起始页+题名”同名 PDF，即使所有 JPG 已是目标名称也重新生成；起始页或题名变更后遗留的旧 PDF 和其他文件保留，不自动清理。
- 本次实现默认的 PDF 预检与失败边界：首张图片改名前检查绑定、图片页码、JPEG 完整解码和 PDF 目标路径冲突；批量任务先检查全部文件夹。执行中错误立即停止，显示已改名、未变化和已生成 PDF 数量，不回滚；单个 PDF 先写同目录临时文件，完整写入后才替换同名目标，失败保留原有 PDF。
- 本次 PDF 技术决定：使用 ReportLab 3.6.13 和 Pillow 9.5.0，固定于 Python 3.8 交付链；原 JPEG 嵌入 PDF 不重压缩，每张图片一页，遵循 EXIF 旋转/镜像，按图片尺寸及 DPI 排版，无有效 DPI 时用 72，不强制 A4。pypdf 4.3.1 仅为构建侧测试依赖，不是客户机运行依赖。
- 已确认图片完整性前提：业务保证页次范围对应的 JPG 不会缺失，首版不设计补页、跳页或缺页后的部分执行流程。
- 已确认规范输入前提：业务保证 `文件题名` 和其他输入满足 Windows 文件名及任务表规范；非法字符等人工录入错误不在首版处理范围内，不做自动清洗或专门恢复。
- 已确认执行交互：用户点击执行按钮后，必要绑定校验通过即直接执行；不弹确认框，不提供复杂预览或逐工作表二次确认。
- 已确认图片选择流程：控件顺序为选择 Excel、选择文件夹、选择工作表；同一 Excel 可在连续任务中保持选中。选择文件夹后按提取出的序号去除前导零，自动选中同名数字工作表，用户仍可手动修改；执行时始终重新校验绑定。
- 已确认批量图片流程：用户在独立标签页选择图片任务表和图片任务父目录；程序遍历全部数字工作表，按编号自动唯一匹配父目录下提取序号相同的直属文件夹（标准数字或编码来源前缀）。非数字工作表及无对应工作表的其他文件夹忽略；缺少或重复匹配以及任一图片计划错误会在首张图片改名前阻止整批执行。
- 已确认空工作表处理：批量图片任务中，数字工作表第四行首个数据行完全为空时直接跳过，不要求对应文件夹存在，也不阻止其他工作表继续执行；结果区域输出跳过数量和工作表名称。非空工作表的错误表头或任务数据仍属于校验失败。
- 已确认页面组织：使用同一个主窗口中的四个标签页；第四页升级为“批量整理档案”，选择文件目录 Excel、图纸目录 Excel 和父目录，执行工作表编排、文件夹改名、图片改名及 PDF 生成。
- 已确认项目架构：采用轻量模块化单体，分为 Tkinter UI、应用服务、纯业务规则和基础设施适配四部分；不引入数据库、网络服务、插件系统、依赖注入框架或回滚模块。详见 `docs/architecture/system-architecture.md`。
- 已确认失败处理：文件系统操作失败时立即停止后续操作，并显示失败对象和错误原因；已完成的操作不自动回滚，不复制备份，用户排除问题后重新执行。
- 已确认 Win7 验证边界：开发侧验证代码、依赖收集、便携目录完整性及当前构建环境运行；用户负责把整个交付目录原样复制到另一台 64 位 Win7 SP1 电脑完成最终兼容性测试。实测前不得声称 Win7 已验证通过。
- 已确认正式版结果提示：不弹成功确认框，在当前标签页底部显示完成数量；图片任务增加 PDF 完成数量和保存位置，失败时显示失败步骤、对象、原因及已完成统计。
- 已确认验收基线：文件夹占位创建与补名、单文件夹和多文件夹图片页次重命名及自动 PDF、错误绑定与批量映射拦截、测试版控制台、正式版无控制台，以及完整目录复制到离线 Win7 的用户实测。详见 `docs/requirements/acceptance-scenarios.md`。

不得把尚未确认的内容写成既定事实。开始首个业务功能前，应先比较可行技术路线、实现难度、依赖成本和测试方式，再记录最终决定。

## 2. 项目记忆维护

- 本文件保存稳定约束、当前阶段、关键技术决定、验证入口和下一步工作。
- 每次完成会改变项目状态的重要功能时，同步更新“当前阶段”、验证命令和必要的技术决定。
- 详细设计或较长执行记录放入 `docs/`，本文件只保留摘要和可追溯文件路径。
- 回答项目状态时，以当前 Git 状态、代码、测试结果和本文件的交叉核对为准；若信息冲突，先查证再更新，不得猜测。

## 3. 强制 Git 分支规则

- `main` 始终保持可发布、可回退状态；除首次空仓库初始化外，禁止直接在 `main` 上开发或提交功能。
- 每项功能、修复、重构或文档工作都必须从最新 `main` 创建独立新分支。
- 自动化代理使用 `agent/<简短任务名>`；人工开发可使用 `feature/`、`fix/`、`refactor/`、`docs/` 或 `chore/` 前缀。
- 一个分支只处理一个明确任务。不得静默带入无关改动，也不得覆盖用户已有的未提交改动。
- 开始工作前执行：

```powershell
git switch main
git pull --ff-only origin main
git switch -c agent/<task-name>
```

- 禁止对共享分支执行强制推送，禁止使用 `git reset --hard`、`git clean -fd` 等破坏性命令处理不明确的工作区。
- 禁止提交密钥、令牌、真实用户数据、临时 Excel 锁文件、虚拟环境、缓存、构建产物和本地 IDE 配置。

## 4. 开发与验证门槛

- 修改前先确认需求是否存在错误前提、信息缺失或未经验证的判断。
- 技术路线未明确时，先评估候选方案的可行性、实现难度和维护成本；技术路线明确后，按既定方案准确、完整地实现。
- 新功能和缺陷修复应提供与风险相匹配的自动化测试；暂时无法测试的部分必须说明原因和人工验证方法。
- 提交前至少执行：

```powershell
git diff --check
git status --short
```

- 项目建立正式测试命令后，必须把命令补充到本节，并在每次合并前执行。
- 只有在相关验证全部通过、工作区范围清楚、文档已同步且无已知阻断问题时，任务才算“开发完成”。

## 5. 开发完成后的自动收尾授权

用户已授权：任务达到上述完成门槛后，无需再次询问是否提交、推送、发布、合并和删除任务分支，应自动完成以下流程：

1. 复核 `git status` 与完整差异，只暂存本任务文件。
2. 使用清晰、简短且能说明目的的提交信息提交。
3. 推送当前任务分支并设置上游。
4. 创建面向 `main` 的 Pull Request，写明改动、原因、影响和验证结果。
5. 确认 PR 可合并且所有必需检查通过后合并。
6. 删除远端任务分支；切回并快进更新本地 `main`；删除本地任务分支。
7. 最后核验本地 `main` 与 `origin/main` 一致、工作区干净，并向用户报告提交、PR、验证和清理结果。

默认使用普通合并策略中的仓库既定方式；仓库尚无既定方式时，优先使用 squash merge，使每个任务在 `main` 上保留一个聚合提交。

以下情况必须停止自动合并并明确报告，而不是绕过门槛：

- 测试或必需检查失败；
- PR 存在合并冲突或审查要求尚未解决；
- 工作区混有无法确认归属的改动；
- 发现疑似密钥、隐私数据、大型二进制文件或不应公开的内容；
- 任务范围或验收标准存在会实质改变结果的歧义；
- GitHub 权限、分支保护或外部服务阻止操作。

## 6. 当前验证入口

项目使用 Python 3.8 标准库 `unittest`，从仓库根目录执行：

```powershell
$env:PYTHONPATH = "$PWD\src"
& "$PWD\.venv\Scripts\python.exe" -m unittest discover -s tests -v
& "$PWD\.venv\Scripts\python.exe" -m compileall -q src tests
& "$PWD\.venv\Scripts\python.exe" -m pip check
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$PWD\packaging\build-portable.ps1"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$PWD\packaging\verify-portable.ps1"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$PWD\packaging\package-release.ps1"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$PWD\packaging\verify-release-archives.ps1"
git diff --check
git status --short
```

首次准备 UCRT 时执行 `packaging/prepare-ucrt.ps1`，它只把微软官方 Windows SDK 组件下载并解包到项目 `.tools`，不安装到客户机。测试、编译检查、依赖检查、双目录构建和发布 ZIP 解压自检必须全部通过；便携自检实际创建两张临时 JPEG 并生成 PDF，而非只检查模块导入，单文件夹及批量业务路径由自动化回归覆盖。真实 Win7 兼容性仍由用户在离线 64 位 Windows 7 SP1 客户机上验证。

## 7. 下一步

v0.4.2 档号名称表驱动文件夹命名的开发侧实现和本地发布归档已完成。下一步：

1. 用户把整个正式版目录复制到另一台离线 64 位 Windows 7 旗舰版 SP1 电脑；
2. 用户按 `docs/requirements/acceptance-scenarios.md` 执行真实目标机测试，包含自动 PDF；
3. 若目标机发现兼容问题，保留测试版控制台输出并从最新 `main` 创建独立修复分支。
