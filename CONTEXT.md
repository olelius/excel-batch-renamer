# Excel 批量文件整理

本上下文描述由任务表驱动的文件夹创建、文件夹重命名和 JPG 图片重命名业务语言。

## Language

**任务表**：
用户为一次批处理选择的 `.xlsx` 文件，其中记录待执行操作所需的指令数据。
_Avoid_：Excel、表格文件、配置表

**序号**：
任务表中从 1 连续到 n 的正整数标识；形成标准文件夹名时按三位显示，不足三位时在左侧补零。
_Avoid_：编号、索引、行号

**文件夹名称**：
任务表中不包含序号和分隔符的文件夹业务名称；文件夹创建阶段允许暂时为空。
_Avoid_：完整名称、目录名

**标准文件夹名**：
由固定三位序号、两个中文破折号 `——` 和非空文件夹名称组成的完整名称，例如 `001——规划管理文件材料`。
_Avoid_：格式化名称、新名称

**占位文件夹**：
文件夹名称尚未确定时，仅由三位序号和两个中文破折号组成的合法文件夹，例如 `001——`。
_Avoid_：空文件夹、未命名文件夹、异常文件夹

**无变更项**：
目标名称与当前名称相同，经过验证但不需要执行文件系统修改的任务项。
_Avoid_：跳过项、失败项、漏处理

**任务模式**：
一次运行中唯一允许执行的批处理类型，只能是文件夹创建、文件夹重命名或图片重命名之一。
_Avoid_：功能、操作类型

**任务目录**：
用户在当前任务模式中单独选择并限定本次文件系统操作范围的文件夹；不同任务模式之间不共享该选择。
_Avoid_：全局目录、固定目录、根目录

**文件夹创建任务**：
根据任务表中的指令，在当前任务目录下批量创建文件夹的任务模式。
_Avoid_：生成文件夹、新建目录

**文件夹重命名任务**：
按三位序号前缀将任务表与本项目创建的现有文件夹唯一匹配，并批量补全或修改文件夹名称的任务模式。
_Avoid_：修改文件夹、目录改名

**图片重命名任务**：
在所选文件夹范围内批量修改 JPG 图片名称的任务模式。
_Avoid_：JPG 改名、照片改名

**图片页码**：
本次执行时图片当前文件名开头的三位数字，例如 `001.jpg` 或 `001旧文件题名.jpg` 中的 `001`；不同执行之间允许发生变化。
_Avoid_：图片序号、任务表序号

**文件题名**：
图片任务表中用于形成图片目标名称的档案文件名称，精确列名为 `文件题名`。
_Avoid_：文件提名、图片名称、文件名

**工作表编号**：
图片任务表中某个工作表的不补零数字名称，其数值标识该工作表对应的项目文件夹。
_Avoid_：工作表序号、任务表序号

**工作表文件夹绑定**：
工作表编号的数值与所选文件夹前三位序号相等的唯一对应关系，例如工作表 `1` 对应文件夹 `001——`。
_Avoid_：工作表选择、目录映射

**页次**：
图片任务表中描述一份文件起始页或完整页码范围的值；单独页码的结束页由下一行起始页推导，最后一行使用显式范围。
_Avoid_：序号、图片数量

**页次区间**：
一份文件所覆盖的连续图片页码范围，起始页和结束页均包含在内。
_Avoid_：图片数量、行范围
