# 自动 PDF 文档协作记录

- 日期：2026-09-08。
- 分支：`agent/image-pdf-export`；子代理只更新文档，未提交或切换分支。
- 用户需求：图片按 Excel 页次改名后自动生成同一文件的 PDF，例如验收文件 6-10 生成五页 PDF；沿用现有页面。
- 文档范围：README、领域术语、AGENTS、功能需求、代表性验收、架构、便携构建说明和交付目录 README。
- 已写明本次默认：同工作表同题名升序归并、原图片目录输出并保留 JPG、重跑覆盖当前同名 PDF、旧题名 PDF 保留、整批前置校验、单 PDF 完整写入后替换及失败停止统计。
- 术语调整：图片任务包含自动生成“文件 PDF”，“无变更项”只描述图片改名，不跳过 PDF 生成；使用 domain-modeling 指导保持 `CONTEXT.md` 为术语表。
- 顺带消除已有表述矛盾：数据连续性从各任务规定起始行计算，文件夹第二行、图片第四行。
- 验证边界：主代理已报告 93 项自动化测试、compileall、pip check 通过；最终便携/ZIP 结果由主代理更新。便携 smoke 使用两张真实临时 JPEG 生成 PDF，不重复单双业务回归。
- 用户要求减少重复检验：保留一轮必要回归、既有双目录/ZIP 自检与代表性 Win7 人工验收，不增加测试矩阵。
- 真实 Win7 SP1 x64 仍待客户机实测；ReportLab 3.6.13 与 Pillow 9.5.0 仅表述为本次固定依赖，不推断其他版本的 Win7 支持状态。
- 文档完成后执行 `git diff --check`，退出码 0；未重复运行应用测试或构建。
